import 'package:flutter/material.dart';

class NotesManagerScreen extends StatelessWidget {
  const NotesManagerScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Notes Manager coming soon'));
  }
}